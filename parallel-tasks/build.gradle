apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'org.testng', name: 'testng', version: '6.11'
}

test {
    useTestNG() {
        parallel "instances"
        threadCount 5
        include "MyTest.class"
    }
}